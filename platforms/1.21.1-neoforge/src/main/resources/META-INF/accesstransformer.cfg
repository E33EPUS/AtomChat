# Fabric access widener entries translated to NeoForge access transformers.
# AtomChatScreen declares its own hidden EditBox/CommandSuggestions fields, so
# only the private selection indexes and the stats map need widening.

public net.minecraft.client.gui.components.EditBox cursorPos
public net.minecraft.client.gui.components.EditBox highlightPos
public net.minecraft.stats.StatsCounter stats
