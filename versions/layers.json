{
  "_comment": [
    "共享层的声明。【一个层就是对目标的一个谓词】：满足它的目标，才把这一层的代码编进去。",
    "",
    "【三个轴】",
    "  since      钉住版本：Minecraft 版本不低于它的目标才挂",
    "  loader     钉住加载器：这个加载器的目标才挂",
    "  mappings   钉住映射家族：用这套映射名的目标才挂",
    "至少写一个。一个都不写就是「全部目标」，那是 shared/，不是层。",
    "",
    "【禁哪个轴 = 哪个轴没被钉住】",
    "  只写 since            版本层：该批版本、各加载器共用      → 层里不许有绑加载器或绑映射的东西",
    "  只写 loader           加载器层：该加载器、各版本共用      → 层里不许有绑版本或绑映射的东西",
    "  只写 mappings         映射层：该映射家族、各加载器共用    → 层里不许有绑版本或绑加载器的东西",
    "  写了多个轴            交集层：两个/三个轴都钉住，都不禁",
    "",
    "【目录是推出来的，不是声明的】",
    "  只写一个轴 → layers/<轴名>/<层名>/      例如 layers/version/1.21+、layers/mapping/official",
    "  写了多个轴 → layers/<轴名按字母序用 + 连接>/<层名>/",
    "本仓与 mcphone 的一处差别：它只有两个轴，所以「两个都写」叫 layers/both/；我们有三个轴，",
    "「both」会歧义（是哪两个），因此改成把轴名拼进目录名。声明得出来、编不过去的那种矛盾，",
    "就是靠「目录由声明推出来」消掉的 —— 没有第二处可以互相矛盾。",
    "",
    "【层名是「这套 API 从哪个 Minecraft 版本开始有」/「哪套映射」，不是某一个具体版本】",
    "1.21.1 与将来的 1.21.x / 更高版本都挂 1.21+，因为它们装的是同一份代码。",
    "",
    "【populated：这一层现在该不该有内容】",
    "true 时层里一个文件都没有就红；声明为 false 却扫到内容也红。层可以先声明后填，",
    "但「空」要是显式声明的状态，不能默认容忍。",
    "",
    "【层由编译证明，不由眼看】一段代码进加载器层的条件是：正文跨两个 Minecraft 版本逐字相同，",
    "差异只在加载器 import —— 版本中立是证得的。映射层同理：它的正文来自「Neo 与 Forge 逐字相同、",
    "只有 Fabric 不同」那一批，两端各自编译通过是既成事实。"
  ],

  "1.21+": {
    "since": "1.21",
    "populated": false,
    "note": "版本层：1.21 起才有的原版 API。1.20.1 不挂载这一层。待填。"
  },

  "official": {
    "mappings": "official",
    "populated": true,
    "note": "映射层：写给 Mojang 官方名的那一份。Fabric 用 Yarn，同一段逻辑的两套名字就是「映射接缝」：该层由所有 official 映射的目标挂载（NeoForge 1.21.1 与 Forge 1.20.1），Fabric 不挂、保留自己那份 Yarn 名副本。两边的文件路径一一对应，由 tools/verify_targets.py 的孪生副本检查盯着（少一个就红）。"
  },
  "fabric": {
    "loader": "Fabric",
    "populated": false,
    "note": "加载器层：Fabric API / loader 专有（与映射无关的那部分）。待填。"
  },
  "neoforge": {
    "loader": "NeoForge",
    "populated": false,
    "note": "加载器层：ModDevGradle + neoforged 专有（载荷注册、事件总线、配置目录来源）。待填。"
  },
  "forge": {
    "loader": "Forge",
    "populated": false,
    "note": "加载器层：ForgeGradle + minecraftforge 专有（SimpleChannel 载荷、Capability、SRG 运行时名）。待填。"
  }
}
